<snippet>
  <content><![CDATA[
// JSLint settings:
/*global alert, console, jQuery, Zepto*/

var ${1:APP} = (function(\$, window, document, undefined) {
  \$(document).ready(function() {
    ${1:APP}.go();
  });

  // "Private constant" vars.
  var body;

  return {
    // ${1:APP}.go
    go: function() {
      var i, j = ${1:APP}.init;

      for (i in j) {
        // Run everything in ${1:APP}.init
        j.hasOwnProperty(i) && j[i]();
      }
    },
    // ${1:APP}.init
    init: {
      // ${1:APP}.init.assign_dom_vars
      assign_dom_vars: function() {
        body = \$(document.body);
      }
    },
    // ${1:APP}.misc
    misc: {
      // ${1:APP}.misc.do_something
      do_something: function() {
        // Not run on page load, but used later?
      }
    }
  };

// Parameters: Zepto/jQuery, window, document.
})(typeof Zepto === 'function' ? Zepto : jQuery, this, this.document);
]]></content>
  <tabTrigger>module</tabTrigger>
  <scope></scope>
</snippet>